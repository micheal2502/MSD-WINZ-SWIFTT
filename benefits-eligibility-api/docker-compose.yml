version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: benefits_db
    environment:
      POSTGRES_DB: benefits_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: benefits_api
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:postgresql://db:5432/benefits_db
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
    depends_on:
      db:
        condition: service_healthy
    restart: on-failure

volumes:
  postgres_data:
